'use strict';

import { createAction } from 'redux-actions';

export const CHANGE_CHATBOX_WIDTH = createAction('CHANGE_CHATBOX_WIDTH');

export const UPDATE_CHAT_FIELD = createAction('UPDATE_CHAT_FIELD');
